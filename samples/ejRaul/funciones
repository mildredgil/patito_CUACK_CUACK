programa patito;
    var ent a,b;
    deci c,d;

funcion ent nose(ent x, ent y) {
    si(x>y) {
        regresa(10);
    }
    sino{
        regresa(-15);
    }
}

funcion deci que(ent x, ent y){
    regresa (x*y/x*x*y*y);
}

funcion letra ago(ent x) {
    si(x>5){
        regresa('z');
    }
}

principal () {
    escribe("Dame dos numeros");
    lee(a,b);
    c=11+3+nose(a,b);
    d=que(a,b);
    escribe(ago(a));
}