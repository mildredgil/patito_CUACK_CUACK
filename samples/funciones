programa patito;
var ent b;
    deci k;
    letra z;

  funcion ent suma(ent i, ent k)
  var ent j, l, m;
      letra a;
  {
    j = i + k / i * l * m - m;
    regresa(j);
  }


  funcion nada mifun(deci m) 
  {
      m = 3.14;
  }
  

  funcion letra leta(letra a)
  var letra d;
  {
    d = a;
    regresa(a);
  }

  principal () {
    b = suma(1,2) + suma(4,5);
    z = leta('a');
    mifun(3.14);
  }