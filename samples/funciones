programa patito;
var ent b;
    deci k;
    letra z;

  funcion ent suma(ent i, ent k)
  var ent j, l, m;
      letra a;
  {
    j = i + k / i * l * m - m;
    regresa(j);
  }

  funcion letra leta(letra a)
  var letra d;
  {
    d = a;
    regresa(a);
  }

  principal () {
    b = suma(1,2);
    z = leta('a');
  }