programa patito;
  var ent arr[10], i, k;

  principal () {
    lee(k);
    # que encuentre k

    arr[0]=0;
    arr[1]=1;
    arr[2]=2;
    arr[3]=3;
    arr[4]=4;
    arr[5]=5;
    
    desde i = 0 hasta 6 hacer {
      escribe("estamos en", i, "y vemos", arr[i]);
      si (arr[i] == 0) {
        escribe(i);
      }
    } 
  }