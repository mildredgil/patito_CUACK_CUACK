programa programaaaaaaaaa;
  var ent z,y;

funcion lerta nose(ent x, ent y) {
    si(x>y) {
        regresa(10);
    }
    sino{
        regresa(-15);
    }
}

funcion deci que(ent x, ent y){
    regresa (x*y/x*x*y*y);
}

funcion letra ago(ent x) {
    si(x>24){
        escribe("El abesedario solo tiene 24 letras");
        regresa('a');
    }
}

  principal () {
    escribe("hola");
  }